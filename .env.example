# MicroHub API Keys
# Copy this file to .env and fill in your keys.
#
# The pipeline uses these APIs across multiple stages:
#
# ENRICHMENT (3_clean.py --enrich / default API enrichment):
#   - OpenAlex:         First-call enrichment: institutions, topics, citations, OA
#   - GitHub:           Repository stats, health scores, activity
#   - Semantic Scholar: Citation counts, fields of study (batch endpoint)
#   - CrossRef:         Data repository discovery (no key needed)
#   - DataCite:         Dataset DOI resolution (no key needed)
#   - OpenAIRE:         Dataset-publication link discovery (no key needed)
#
# FULL-TEXT ACQUISITION (three-tier waterfall):
#   - Europe PMC:   Tier 1 — JATS XML full text (no key needed)
#   - Unpaywall:    Tier 2 — OA PDF discovery (no key needed, email required)
#   - GROBID:       Tier 2 — PDF processing (local service, no key needed)
#
# VALIDATION (automatic in orchestrator):
#   - FPbase:       Fluorescent protein validation (no key needed)
#   - SciCrunch:    RRID validation (no key needed)
#   - ROR v2 API:   Organization affiliation matching (client ID recommended)
#   - NCBI Taxonomy: Organism validation (no key needed)
#   - PubTator 3.0: Supplemental NER for papers with PMIDs (no key needed)
#
# LLM VERIFICATION (3_clean.py --enrich --ollama):
#   - Ollama:       Local LLM for Methods section cross-checking (no key needed)

# GitHub Personal Access Token (https://github.com/settings/tokens)
# Without token: 60 API calls/hour.  With token: 5,000/hour.
# No special scopes needed — public repo access only.
GITHUB_TOKEN=

# Semantic Scholar API Key (https://www.semanticscholar.org/product/api#api-key)
# Without key: ~1 request/sec, heavily rate-limited.
# With key: ~10 requests/sec, batch endpoint works reliably.
SEMANTIC_SCHOLAR_API_KEY=

# OpenAlex API (https://docs.openalex.org)
# Email for polite pool (faster rate limits).  API key required since Feb 2026.
# Singleton lookups are free without key.  List queries: 100K credits/day.
OPENALEX_EMAIL=
OPENALEX_API_KEY=

# ROR API client ID (https://ror.org/api-client-id)
# Without client ID: 50 requests per 5 minutes.
# With client ID: 2,000 requests per 5 minutes.
ROR_CLIENT_ID=

# Unpaywall email (required for API access, used for rate limit tracking)
# Rate limit: 100,000 calls/day per email.
UNPAYWALL_EMAIL=

# Ollama local LLM (https://ollama.ai)
# Install: curl -fsSL https://ollama.ai/install.sh | sh
# Pull model: ollama pull llama3.1
# The agent connects to Ollama's HTTP API — no API key needed.
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1
