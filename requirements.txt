# Core HTTP
requests>=2.28.0
aiohttp>=3.8.0
certifi>=2023.0.0

# Data export
openpyxl>=3.1.0

# GROBID PDF parsing (optional -- requires beautifulsoup4 + lxml)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# NLP / entity extraction (optional -- enhances extraction quality)
# scispacy>=0.5.0        # Biomedical NER (install separately: pip install scispacy)
# taxonerd>=1.3.0        # Organism NER (install separately: pip install taxonerd)
# chemdataextractor2     # Chemical NER (install separately)
# spacy>=3.5.0           # NLP pipeline (install separately)

# Testing
pytest>=7.0.0
